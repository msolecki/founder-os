# Demo Tabpanel Batch

Complete `A11Y-006` by making each demo `tabpanel` programmatically focusable with `tabindex="-1"`, preserving the tablist keyboard model while allowing focus to move to the active panel.
