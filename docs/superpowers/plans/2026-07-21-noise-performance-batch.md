# Noise Performance Batch

Complete `PERF-006` by replacing the full-viewport SVG `feTurbulence` texture with a lightweight CSS radial-gradient pattern and pinning the absence of the filter in the landing-page contract tests.
