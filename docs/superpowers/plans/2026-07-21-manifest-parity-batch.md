# Manifest Parity Batch

Complete `ARCH-004` by synchronizing Claude/Codex plugin identity fields and validating `name`, `version`, and `description` agreement.

Evidence target: focused parity test, package validator, command generation, and full unit suite.
